<h2>Events on: <%= Date.today.strftime("%A, %B %e, %Y")%></h2>

<ul>
	<% @events.each do |event| %>
		<li>
		  <%= event.start_date_time.strftime("%l:%M %p") %>
		  <%= event.away_team.full_name %>
		  <%= event.home_team.full_name %>
		  <%= event.event_status %>
		 </li>
	<% end %>
</ul>